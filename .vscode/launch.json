{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run Project",
            "program": "${workspaceFolder}/src/app.ts",
            "preLaunchTask": "tsc: build - tsconfig.json",
            "smartStep": true,
            "outFiles": [
                "${workspaceFolder}/compiled/**/*.js"
            ]
        }
    ]
}